"scripts": {
    "build": "vite build",   ← precisa ter isso
    ...
}
```

Se não tiver, substitua pelo `package.json` original do frontend que você me enviou no início da conversa (aquele com `vite`, `react`, `firebase`, etc.).

---

## Passo 2 — Crie o `.dockerignore` na raiz

No GitHub → **Add file → Create new file** → nome: `.dockerignore`

Conteúdo:
```
node_modules
dist
.env
.env.*
.git
*.log
coverage
proxy-backend
```

---

## Passo 3 — Confirme a estrutura correta na raiz

O repo deve ter:
```
├── .github/workflows/deploy.yml
├── .dockerignore          ← criar agora
├── Dockerfile             ← frontend (nginx)
├── nginx.conf             ← já criado
├── package.json           ← frontend (com vite build)
├── src/
├── components/
├── services/
└── proxy-backend/         ← pasta separada do proxy
